# 基础配置
bind 0.0.0.0
port 6379
protected-mode no

# 持久化配置
appendonly yes
appendfsync everysec

# 性能优化
maxmemory 256mb
maxmemory-policy allkeys-lru

# 日志
loglevel notice

# 密码（生产环境建议启用）
# requirepass your_strong_password_here