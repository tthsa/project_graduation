server:
  port: 8080

spring:
  application:
    name: code-review-backend
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 2000ms

# 硅基流动配置
siliconflow:
  api-key: "sk-okpdxfwbttwgrmjbjjycpljgsycroeihfreuwaarqldsodfs"
  base-url: "https://api.siliconflow.cn"
  model: "deepseek-ai/DeepSeek-V3.2"
  timeout: 60000

# 评审配置
review:
  stream:
    key: "code-review-stream"
    group: "llm-review-group"
  result:
    ttl: 86400